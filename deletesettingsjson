{
    // ─── Agent Skills ────────────────────────────────────────────
    // Enable native VS Code Agent Skills discovery (GA in 1.109)
    "chat.useAgentSkills": true,
    "chat.useNestedAgentsMdFiles": true,

    // Tell VS Code to also discover skills from .claude/skills/
    "chat.agentSkillsLocations": {
        ".claude/skills": true
    },

    // ─── Subagents & Orchestration ───────────────────────────────
    // Allow custom agents to be invoked as subagents (1.109)
    "chat.customAgentInSubagent.enabled": true,

    // Dedicated search subagent for complex codebase queries (1.109)
    "github.copilot.chat.searchSubagent.enabled": true,

    // ─── Interactive Tools ───────────────────────────────────────
    // Agent can ask clarifying questions via native UI (1.109)
    "chat.askQuestions.enabled": true,

    // Agent customization skill - teaches agent about agents/skills/instructions (1.109)
    "chat.agentCustomizationSkill.enabled": true,

    // ─── Copilot Memory ──────────────────────────────────────────
    // Persist learnings across sessions (Preview, 1.109)
    "github.copilot.chat.copilotMemory.enabled": true,

    // ─── Anthropic Model Enhancements ────────────────────────────
    // Show thinking tokens for better reasoning visibility (1.109)
    "github.copilot.chat.anthropic.thinking.budgetTokens": 10000,

    // Tool search for better tool selection from large pools (1.109)
    "github.copilot.chat.anthropic.toolSearchTool.enabled": true,

    // Context editing — clears stale tool results for longer conversations (1.109)
    "github.copilot.chat.anthropic.contextEditing.enabled": true,

    // ─── Security & Trust ────────────────────────────────────────
    // Terminal sandbox — restrict agent commands to workspace (Linux/macOS, 1.109)
    // Requires: ripgrep, bubblewrap (bwrap), socat. Enable when available.
    // "chat.tools.terminal.sandbox.enabled": true,

    // ─── Organization ────────────────────────────────────────────
    // Apply org-level custom instructions automatically (1.109)
    "github.copilot.chat.organizationInstructions.enabled": true
}